{
  "name": "@stellium/monolog",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prestart": "npm run clean",
    "start": "tsc --watch",
    "build": "npm run compile",
    "clean": "npm run rimraf -- dist",
    "rimraf": "rimraf",
    "precompile": "npm run clean",
    "compile": "tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/amqplib": "^0.5.1",
    "@types/colors": "^1.1.3",
    "@types/node": "^7.0.18",
    "@types/request": "0.0.43",
    "rimraf": "^2.6.1",
    "typescript": "^2.3.2"
  },
  "dependencies": {
    "@stellium/common": "github:stellium/common",
    "amqplib": "^0.5.1",
    "colors": "^1.1.2",
    "containerized": "^1.0.2",
    "request": "^2.81.0"
  }
}
